# R for travis: see documentation at https://docs.travis-ci.com/user/languages/r

language: r
cache: packages
before_install:
  - sudo apt-get install -y r-cran-gsl
addons:
  apt:
    packages:
      - libgsl2
r_binary_packages:
 - gsl
