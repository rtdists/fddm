# R for travis: see documentation at https://docs.travis-ci.com/user/languages/r

language: r
r: 4.0.2
cache: packages
dist: bionic
addons:
  apt:
    packages:
      - libgsl2
r_binary_packages:
 - gsl
